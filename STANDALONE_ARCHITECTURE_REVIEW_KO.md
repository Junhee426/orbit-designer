# 브라우저 단독 실행 중심 통합 검토

> 구현 상태 업데이트: 브라우저 통합판을 `standalone/`에 구현했다. J2·Multi-shell·TLE, 한국 기본 선택, 모든 지표, 기간·후보 분석, 스키마 v2 저장·복원을 제공한다. 화면은 배포된 Test Orbit Designer의 상단 작업 단계, 화면별 좌측 설정, 요약·지도·상세 결과 흐름을 기준으로 확장했다. 초기 화면과 기간 요약은 통신망을 먼저 제시하고, 항법 탑재체·오차 설정과 HRMS·VRMS·PDOP 결과는 펼침 영역에서 확장한다. 3D·2D 지도는 지구 이미지와 Natural Earth 기반 윤곽선 전용 보기를 전환할 수 있다. 표와 내보내기에는 통신·항법 지표를 모두 유지한다. 실행법과 실제 지원 범위는 [standalone/README.md](standalone/README.md)를 따른다. 아래 내용은 구현 전 검토 기록이며, 설치형 PWA와 자동 저장은 이번 구현에 포함하지 않았다.

2026-09-14. 이 문서는 통합 실행 구조의 검토이며, 현재 구현 완료 사항은 기존 Orbit Designer의 한국 단독 초기 선택이다.

## 권고

통합판의 기본 구조는 **정적 웹 앱 + 브라우저 계산 엔진 + Web Worker**가 적합하다. commnav의 실행 구조를 확장하고 Orbit Designer의 궤도·분석·시각화 기능을 가져오는 방향이다. 실행에 FastAPI를 요구하지 않고, 기존 Python 계산 코드는 개발 중 수치 검증 기준으로 활용한다.

요구사항은 다음과 같이 확정한다.

- Walker와 Multi-shell의 J2 모델을 현재 Orbit Designer의 J2 RAAN 보정 수준으로 맞춘다. 전체 J2 섭동 전파기로 확대했다는 의미는 아니다.
- 지도·가시성·통신·항법이 같은 전파 상태를 사용한다. 속도에는 RAAN 변화와 지구 자전 효과도 반영한다.
- 기존 지표를 모두 표시한다. 기하학적 가시성, RF 마진·처리량, 항법 오차, 개별·동시 목표 충족률을 구분하고 정의·단위를 명시한다.
- 최초 국가 선택은 한국만 켠다. 사용자가 다른 국가를 추가하거나 저장된 시나리오를 불러오면 해당 선택을 존중한다.
- 시나리오는 통신·항법 입력, 관측지 출처, 시간·좌표 기준, 모델 버전을 포함하도록 확장한다.

## 단독 실행의 장점

| 장점 | 이 프로젝트에 미치는 효과 | 조건·한계 |
|---|---|---|
| 계산 서버 운영 불필요 | Python 환경·서버 프로세스·계산 API 운영을 사용자 실행 경로에서 제거 | 정적 파일을 전달할 웹 호스팅 또는 로컬 제공 수단은 필요 |
| 사용자별 계산 자원 사용 | 현재 FastAPI의 프로세스당 동시 작업 제한을 여러 사용자가 공유하지 않음 | 사용자 기기 CPU·메모리·발열에 따라 성능이 달라짐 |
| 입력값을 기기 안에서 처리 | 궤도·관측지·성능 가정을 계산 서버에 전송할 필요가 없음 | 지도·외부 자산 요청도 제거해야 완전한 네트워크 독립 실행이 됨 |
| 상호작용 지연 감소 | 시각 이동·슬라이더 변경마다 API 응답을 기다릴 필요가 없음 | 무거운 스윕은 별도 작업으로 실행해야 함 |
| 시연·교육·현장 사용 | 동일한 파일 세트로 여러 PC에서 실행하고, 자산을 준비하면 인터넷 없이 시연 가능 | 현재 앱을 HTML 더블클릭만으로 실행할 수 있다는 뜻은 아님 |
| 버전 재현성 | 엔진·기본값·지도 자산·시나리오 버전을 묶어 보관 가능 | 브라우저 간 수치 비교는 비트 단위 일치보다 허용 오차 기반 검증 필요 |
| 배포 구조 단순화 | 정적 자산 배포로 업데이트 가능 | PWA 캐시에서는 이전 화면과 새 엔진이 섞이지 않도록 버전 관리 필요 |

Web Worker는 UI와 별도 스레드에서 계산할 수 있고, 메시지로 진행률과 결과를 전달한다. 큰 결과를 일반 객체로 반복 복사하기보다 필요한 결과만 보내거나 전송 가능한 버퍼를 사용하는 구조가 적합하다. [MDN Web Workers](https://developer.mozilla.org/en-US/docs/Web/API/Web_Workers_API/Using_web_workers)

## 현재 코드에서 확인한 근거

commnav는 런타임 외부 API·CDN 없이 로컬 이미지와 ES 모듈을 사용하며 24시간 계산을 Worker로 분리했다. 정적 파일 전체는 약 518 KB다. 이는 현재 작은 Canvas 앱의 크기이며 Cesium까지 포함한 통합판 크기가 아니다.

현재 `engine.js`를 Node v24.19.0에서 직접 실행해 서울·기본 통신/항법 설정·5분 간격 288개 표본을 한 번씩 측정했다.

| LEO 위성 수 | GNSS 포함 위성 수 | 전파·평가·집계 시간 | JSON 결과 크기 |
|---|---:|---:|---:|
| 256 | 280 | 약 60 ms | 85,535 bytes |
| 512 | 536 | 약 72 ms | 85,864 bytes |

현재 규모의 브라우저 계산 가능성을 뒷받침하는 참고 측정이다. **Node 단회 실행이며 브라우저 벤치마크가 아니다.** J2, 지도 렌더링, Worker 시작·메시지 전달, 다지역 히트맵, ISL 경로 및 대규모 후보 비교는 포함하지 않았다. 이 수치로 모바일 속도나 통합판의 응답 시간을 보장하지 않는다.

## J2는 단독 실행을 막는 요인이 아니다

현재 Python J2 모델은 일정한 RAAN 변화율을 계산하여 `RAAN(t) = RAAN(0) + rate × t`로 적용한다. 이 수식을 JavaScript 공통 전파기로 옮기는 데 Python 서버가 필요하지 않다. 위성별 계산 복잡도의 차수도 그대로다.

도플러까지 맞추려면 위치의 시간 미분에 RAAN 변화율을 포함하고, ECI→ECEF 변환 시 지구 자전 항을 적용해야 한다. 지도만 J2 궤도로 바꾸고 commnav 속도를 그대로 두면 통신 도플러가 불일치한다. 기존 Python 위치와 비교하고, 위치·경사거리의 중심 차분으로 속도·거리 변화율을 검증한다.

J2 끔은 기존 결과를 확인하는 검증 옵션으로 남기고 통합판의 기본은 J2 켬으로 한다. TLE/SGP4는 자체 전파 모델을 사용하며 Walker의 J2 식을 중복 적용하지 않는다. 이상화 GNSS·지역항법군은 별도 모델로 명시한다.

## 옮길 때 실제로 큰 작업

1. **가시성·히트맵·기간 집계 이식:** 현재 NumPy 배열 계산을 JavaScript의 청크 계산으로 옮긴다. 위성×관측지×시간×후보를 전부 메모리에 보관하지 않는다.
2. **TLE/SGP4 이식:** 현재 Python `sgp4` 호출을 브라우저에서 실행할 구현으로 대체하고 기존 TLE 검증 자료로 비교한다. 구현 라이브러리 선정과 검증은 별도 작업이다.
3. **ISL·경로·다후보 비교:** 전체 기능을 유지하려면 그래프 계산도 옮겨야 한다. Worker 사용만으로 연산량이 줄지는 않으므로 작업량 예측·진행률·취소·재계산 범위를 설계한다.
4. **국가 카탈로그와 시나리오 검증:** 현재 `/api/service-regions/*`, `/api/scenario/validate`를 정적 데이터와 브라우저 검증 모듈로 대체한다. 직접 입력 관측지와 국가별 자동 생성 관측지를 구별한다.
5. **지도 자산 제공:** Cesium, Worker·Widgets·Assets, 지구 이미지, 경계 데이터를 함께 배포한다. 기존 `/static/` 절대경로와 API 경로를 배포 기준 경로에 맞춰 변경한다.

Cesium은 브라우저에서 동작하는 라이브러리이므로 FastAPI를 필수로 요구하지 않는다. 공식 설치 안내도 정적 자산 위치와 `CESIUM_BASE_URL` 구성을 설명한다. [CesiumJS Quickstart](https://cesium.com/learn/cesiumjs-learn/cesiumjs-quickstart/)

현재 Orbit Designer에는 Cesium CDN 대체 경로와 ArcGIS 영상 옵션이 있으므로, 완전 오프라인 모드에서는 로컬 자산을 선택하고 외부 요청이 없는지 검증해야 한다. TLE는 파일로 불러오면 오프라인 계산할 수 있지만 최신 TLE 자동 수집은 별도 네트워크 기능이다.

## 배포 방식 선택

| 방식 | 사용자 경험 | 평가 |
|---|---|---|
| 정적 웹 앱 | URL 접속 후 브라우저에서 계산 | 기본 배포로 권장. 사용자 Python·Node 설치 불필요 |
| 설치 가능한 웹 앱(PWA) | 온라인 설치·자산 준비 후 앱처럼 열고 오프라인 사용 | 다음 단계로 권장. 캐시·버전·실패 복구 구현 필요 |
| 로컬 파일 묶음 + 정적 파일 제공기 | 배포 폴더를 로컬 HTTP로 열어 사용 | 폐쇄망·고정 버전 시연에 적합. 제공기 준비 또는 실행기 묶음 필요 |

현재 commnav는 모듈·Worker 때문에 HTTP로 실행한다. ‘단독 실행’은 계산 백엔드가 없다는 의미이며 `file://` 더블클릭 지원과 구별한다. 기존 `npm start`는 로컬 HTTP 제공을 위해 Python을 사용하지만, 이것이 수치 계산에 Python이 필요하다는 의미는 아니다.

PWA의 Service Worker로 앱 자산을 캐시하면 오프라인 제공을 구현할 수 있다. 배포는 HTTPS가 필요하고 localhost는 개발 예외다. 현재 commnav에는 Service Worker·설치 manifest가 없으므로 오프라인 설치형 앱으로 완성된 상태는 아니다. 처음부터 인터넷이 없는 PC에는 필요한 자산을 포함한 로컬 배포물을 제공해야 한다. [MDN Service Workers](https://developer.mozilla.org/en-US/docs/Web/API/Service_Worker_API/Using_Service_Workers)

## 권장 데이터 흐름과 지표 화면

```text
시나리오(JSON) · 국가 선택(초기 한국) · 관측지 · 분석 기간
                         │
                    브라우저 상태
                         │
               계산 Worker / 공통 궤도 엔진
                ├─ Walker + J2 / Multi-shell / TLE
                ├─ 가시성 · 히트맵 · ISL·경로
                ├─ 통신 마진 · 처리량 · 지연 · 도플러
                └─ 항법 HRMS · VRMS · PDOP · 목표 충족률
                         │
             Cesium · 하늘보기 · 모든 지표 · JSON/CSV
```

지표는 한 시나리오 안에 모두 제공하되 현재 시각과 기간 집계를 구분한다. 가시성에는 가시 위성 수·시간 비율·최대 표본 단절·핸드오버·재접속, 통신에는 링크 마진·처리량·전파지연·도플러·목표 충족률, 항법에는 구성별 HRMS·VRMS·PDOP·유효해 비율·목표 충족률, 종합에는 통신·항법 동시 목표 충족률을 배치한다. 계산되지 않거나 불능인 값은 0 대신 상태와 이유를 표시한다.

최초 로딩의 한국 선택은 기존 `app/static/app.js`에 반영했다. 통합판에서는 시나리오 로딩 시 저장된 선택을 우선하며, 한국만 선택한다는 요구를 다른 국가 분석 기능 삭제로 해석하지 않는다.

## 검증 계획

- J2 켬/끔 위치 교차 검증, 속도·거리 변화율의 수치 미분 비교.
- 기존 COMM/PNT와 기간 집계 결과 비교, 지표 분모·단위·불능 표본 검증.
- 시나리오 저장/복원 시 모든 설정과 직접 입력 관측지 보존.
- 대표 PC 브라우저에서 128/256/512기, 한국 관측지, 24시간 분석·지도 동시 사용 측정.
- 더 많은 위성·국가·후보에서 메모리·취소·진행률·UI 응답 측정 후 지원 상한 결정.
- 네트워크 차단 재실행, 자산 누락·캐시 업데이트·결과 내보내기 검증.

이번에는 기존 엔진의 Node 참고 측정과 초기 국가 선택 변경만 수행했다. 브라우저 통합 성능, J2 이식, 전체 지표 화면, 확장 스키마, PWA 구현은 이후 통합 작업 범위다.
