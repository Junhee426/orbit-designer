# 코드 검토 및 지구 확대·축소 변경

검토일: 2026-09-21

## 검토 항목 — 모두 수정 완료

### P2 — 통신 전용 후보 비교도 숨겨진 항법 지표로 정렬됨 → 수정 완료

- 위치: `standalone/analysis.js:48`, `standalone/app.js`의 `renderTrade()`.
- `tradeStudy()`는 항상 통신·항법 동시 충족률인 `joint`를 기준으로 정렬한다. 통신 전용 화면에서는 항법·동시 충족률 열만 숨기므로, 보이지 않는 항법 조건이 후보 순서에 영향을 준다.
- 재현: 기본 시나리오에서 `display.domain='comm'`, `horizontalTarget=0.1`로 하루 분석을 실행하면 모든 후보의 `joint`가 0이다. 통신 충족률 98.26%인 500 km / 128기 후보가 100%인 888 km / 128기 후보보다 먼저 표시된다.
- 수정: 통신 전용은 `comm`, 통신·항법은 `joint` 기준으로 내림차순 정렬한다. 동률은 위성 수, 고도 오름차순으로 정렬한다. 화면 모드를 변경하면 저장된 결과를 복사해 다시 정렬하고 기준을 화면에 표시한다. 내보내기도 현재 표시 순서와 `sort_metric`을 기록한다. 원래 계산 조건과 결과는 유지한다.

### P2 — 경량 평면도에서 ‘관측지 중심’이 적용되지 않음 → 수정 완료

- 위치: `standalone/rendering.js`의 `center()`와 `draw()` 내부 `mapProject()`.
- `center()`는 `lat`, `lon`을 변경하지만 평면도 투영은 이 값을 읽지 않고 항상 경도 0°, 위도 0°를 기준으로 그린다. 관측지를 변경하거나 ‘관측지 중심’을 눌러도 평면도의 중심은 그대로다.
- 영향: 확대 후 화면 밖으로 나간 관측지를 해당 버튼으로 다시 찾을 수 없다. 경량 지구본의 중심 이동에는 해당하지 않는다.
- 수정: 평면도의 별도 중심 좌표를 투영에 반영한다. 관측지 변경과 ‘관측지 중심’은 확대 배율을 유지하면서 해당 관측지를 중앙으로 이동한다. 드래그 이동과 터치 입력을 지원하며 경도는 순환하고 위도는 ±90°로 제한한다. 지도 절단선을 가로지르는 선은 분리하고, 히트맵·풋프린트 영역은 좌우 사본을 잘라 표시해 화면을 가로질러 채워지는 현상을 막는다.

### P3 — 경량 지구본의 배경 캐시가 매 갱신마다 무효화됨 → 수정 완료

- 위치: `standalone/viewer.js`의 `update()`, `standalone/rendering.js`의 `setFull()`.
- 매 갱신 때 `setFull(showNavigation)`을 호출하고, 값이 같아도 배경 캐시를 지우고 즉시 그린다. 이어지는 `set()`도 다시 그리므로 재생 중 불필요한 배경 계산과 렌더링이 발생한다.
- 수정: 같은 항법 표시 값의 재설정을 생략하고, 모드·색상·크기 등 표시 설정을 먼저 적용한 뒤 새 스냅샷으로 한 번만 그린다. 배경은 시점·텍스처·해상도가 같으면 재사용한다. 회귀 테스트에서 120회 갱신은 120회 그리기로 처리되었고 추가 배경 텍스처 생성이 없었다. 시점이나 표시 범위가 바뀌면 새 배경을 생성하는 것도 확인했다. 실제 FPS 개선율은 측정하지 않았다.

## 이번에 수정한 사항

- 브라우저 단독판과 Python 서버판에 접근 가능한 이름과 키보드 포커스 표시가 있는 44×44 px 확대·축소 버튼을 추가했다.
- Cesium 3D/2D는 카메라 거리를, 경량 지구본/평면도는 투영 배율을 조정한다. 한 번에 1.25배씩 변경하며 배율·거리 한도를 둔다.
- 확대·축소는 선택 위성, 시뮬레이션 시각, 분석 입력을 변경하지 않는다.
- TLE 경량 렌더링에서 존재하지 않는 고정 반지름·RAAN 속성을 사용하던 오류를 수정했다. 전파된 위성 위치로 표시 범위를 계산하고, TLE 궤도는 시간별 전파로 그린다.
- Cesium에서 서로 다른 TLE 궤도를 하나의 궤도면으로 묶어 첫 위성 궤도만 그리던 문제도 수정했다.

## 확인 범위와 결과

- 검토 범위: 브라우저·서버 화면, 시나리오 검증/가져오기, 분석 Worker와 결과 처리, JS 궤도·가시성·경로 코드, Python API의 입력/작업량 제한 및 계산 연결, 빌드·정적 배포 경로.
- Python 전체 기존 테스트 165개 통과. 변경 후 관련 프런트엔드 회귀 테스트 12개도 통과.
- JS 테스트 16개 통과: 기존 계산 엔진 12개와 정렬·평면도 중심/드래그·지도 절단선·배경 재사용 회귀 테스트 4개.
- Playwright 8개 통과: 기존 분석·내보내기·모바일 흐름, 네 지도 모드 확대·축소, 시각·위성 선택 유지, WebGL 없는 TLE 경량 표시, 모드 전환 후 후보 재정렬/내보내기, 확대·드래그 후 중심 복원/관측지 변경.
- Python 서버 화면도 실제 브라우저에서 네 지도 모드의 확대·축소를 확인했고 브라우저 오류가 없었다. 서버용 선택 자산이 설치되어 있지 않아, 이 확인에서는 설치된 동일 버전 Cesium 자산을 브라우저 요청에 연결했다.
- 프런트엔드 정적 검사, 빌드, 정적 배포 검사 통과.
- 이번 후속 수정은 브라우저 단독판의 위 세 항목에 적용했다. Python 전체 테스트와 서버 화면 확인은 앞선 확대·축소 변경 시 수행한 결과다. 모든 입력 조합의 수학적 정확성이나 실제 위성망 성능을 새로 검증한 것은 아니다.
