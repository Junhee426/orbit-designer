# Orbit Designer · commnav 통합 검토

> 구현 상태 업데이트: 이번 통합은 브라우저 단독 실행으로 진행했으며 `standalone/`에 구현했다. 실행법·지원 범위·검증 방법은 [standalone/README.md](standalone/README.md)를 따른다. 아래 내용은 의사결정에 사용한 검토 기록이다.

검토일: 2026-09-14. 대상: `C:/kleo/orbit-designer`와 `C:/kleo/commnav`의 현재 로컬 코드. 사용자 요구에 따라 브라우저 단독 실행 중심으로 권고를 수정했다. 현재 앱에는 한국만 초기 선택하도록 반영했으며, 통합 엔진 구현과 배포는 수행하지 않았다.

## 판단

통합을 권장한다. Orbit Designer의 궤도 설계·지역 가시성 분석을 기반으로 commnav의 통신·항법 성능 분석을 추가하면, 같은 위성군에 대해 **배치 → 가시성 → 통신 처리량 → 항법 정확도 → 동시 목표 충족률**을 비교하는 하나의 설계 도구가 된다.

권장 방향은 **브라우저 공통 계산 엔진·Web Worker를 중심으로 통합하고, Cesium 화면과 분석 모듈을 연결**하는 것이다. Walker는 기존 Orbit Designer 수준의 J2 RAAN 보정을 공통 적용하고, 지도·통신·항법에서 같은 위치와 속도를 사용한다. 모든 기존 지표를 구분하여 표시하고, 초기 국가는 한국만 선택한다. 시나리오 구조는 확장한다. 상세 실행 방식 비교는 [단독 실행 검토](STANDALONE_ARCHITECTURE_REVIEW_KO.md)에 정리했다.

## 기능과 유지할 자산

| 영역 | Orbit Designer | commnav | 통합 방향 |
|---|---|---|---|
| 실행 | Python/FastAPI/NumPy, 서버 계산 | JavaScript, 브라우저·Web Worker 계산 | 브라우저 공통 엔진·Worker, Python 코드는 교차 검증에 활용 |
| 궤도 | Walker F 설정, J2 RAAN 보정, Multi-shell, TLE/SGP4 | Walker F=1, J2 없음, 이상화 GNSS·지역항법군 | LEO 전파는 공통화, GNSS·지역항법군 생성 기능은 이식 |
| 지상 분석 | 여러 도시·국가 선택, 가시성·단절·히트맵 | 한 관측지, 통신·항법별 최소 고도각 | 공통 관측지 목록과 개별 관측지 상세 분석 |
| 통신 | 링크 마진 예시, ISL·경로·전파지연 | 단일 사용자 처리량, 도플러, 항법 시간 공유 | 링크 마진과 처리량 모델을 별도 지표로 유지 |
| 항법 | 전용 분석 없음 | GNSS/LEO/지역항법 공분산, HRMS·PDOP | commnav 수치모델·검증 사례 재사용 |
| 설계 비교 | 고도·위성 수 등에 따른 기간 가시성 비교 | 여러 설계변수의 현재 시각 스윕, 24시간 통신·항법 분석 | 후보별 같은 기간의 복수 목표 평가로 확장 |
| 시각화 | Cesium 2D/3D, 궤적·Footprint·ISL | Canvas 지구본, 하늘보기, 분석 그래프 | Cesium을 주 지도로 사용, 하늘보기·그래프 추가 |
| 저장·공유 | 시나리오 JSON, 결과 JSON/CSV, 입력 해시 | URL 설정 공유, 결과 CSV | 버전 있는 통합 시나리오와 결과 메타데이터 |

commnav의 `dist/`는 현재 직접 관리하는 실행 소스다. 일반적인 자동 생성 빌드 폴더로 보고 삭제하거나 제외하면 안 된다. `npm run build`는 파일 생성 대신 검증을 수행한다.

## 직접 확인한 공통 계산 기반

두 프로젝트는 지구 반경 6378.137 km, 중력상수 398600.4418 km³/s², 지구 자전율 7.292115e-5 rad/s를 공유한다. Walker 위상 공식도 F=1에서 같다.

양쪽 실제 모듈을 Python과 Node에서 호출해 다음 조건으로 대조했다.

- 고도 500/888/1280 km × 궤도면 8/16개, 면당 16기: 6개 위성군.
- 경사각 42°, F=1, Orbit Designer J2 끔.
- 시각 0/300/3600/43200/86400초: 총 5,760개 위성 상태.
- 서울 37.5665°N, 126.978°E, 최소 고도각 20°.
- ECEF 위치 벡터 차이 최댓값: 약 `2.45e-12 km`.
- 경사거리 차이 최댓값: 약 `3.64e-12 km`.
- 고도각 차이 최댓값: 약 `9.52e-13°`; 가시/비가시 판정 불일치 0건.

이 비교는 공통 Walker 조건의 기하 계산이 일치한다는 근거다. 통신·항법 결과나 J2·TLE·Multi-shell까지 검증한 결과는 아니다. 같은 비교군에서 J2를 켜면 24시간 시점의 위치 차이가 최대 약 682.27 km였다. 이는 모델 설정 차이로, 계산 오류 판정이 아니다.

## 통합 전에 해결할 차이

### 1. 기본값과 지원 범위

Orbit Designer 기본값은 1280 km·8×16기·J2 켬이고, commnav는 888 km·16×16기·J2 없음이다. 각각의 기본값으로 화면을 열어 같은 시나리오라고 표시하면 안 된다.

commnav는 총 LEO 512기, 고도 400–2000 km, 경사각 0–90°, F=1을 전제로 한다. Orbit Designer는 더 넓은 범위를 받는다. 초기 설정 연동은 양쪽 지원 범위의 교집합으로 한정하고, 지원하지 않는 설정은 명시적으로 차단해야 한다. F나 J2를 조용히 바꾸어 계산하지 않는다.

근거: `app/core/models.py`, `app/main.py:107`, `app/server_config.py`, `C:/kleo/commnav/dist/engine.js:17`.

### 2. 시간·좌표·속도 계약

Orbit Designer는 초, commnav의 분석 인터페이스는 분을 사용한다. 두 Walker 모드의 기준 지구 회전각은 0이지만, TLE는 UTC와 TEME 변환을 사용한다. 공통 데이터에는 시간 기준, 좌표계, 단위와 전파 모델을 기록해야 한다.

commnav의 도플러는 ECEF 속도 벡터에서 구한 관측지 방향 거리 변화율을 사용한다. Orbit Designer의 snapshot에는 위치와 속력 크기가 있지만 ECEF 속도 벡터가 없다. 기존 snapshot JSON을 그대로 전달하는 것만으로 도플러가 보존되지는 않는다. 공통 상태 공급기에 위치·속도 벡터를 추가하고 J2 및 TLE 모드별 변환을 검증해야 한다.

위성 ID도 `P01-S01`과 `L001`처럼 다르다. 공통 ID 및 shell/group/plane/slot을 정의하고, 항법 탑재 여부를 ID와 연결해야 한다. commnav의 현재 탑재 비율 배정은 위성 순서에 의존한다.

근거: `app/core/snapshot.py:46`, `app/core/constellation.py:54`, `C:/kleo/commnav/dist/engine.js:62`.

### 3. 가시성·목표 충족률의 의미와 단위

Orbit Designer의 `availability`는 최소 고도각 이상 위성이 1기 이상 보이는 시간 비율이며 0–1 값이다. commnav의 `commAvailability`는 처리량 목표, `navAvailability`는 항법 HRMS 목표, `jointAvailability`는 두 목표의 동시 충족 표본 비율이며 0–100 값이다.

통합 화면에서는 **기하학적 가시 시간 비율 / 통신 목표 충족률 / 항법 목표 충족률 / 동시 목표 충족률**을 구분한다. 모두를 하나의 ‘가용성’ 카드로 대체하지 않는다.

Orbit Designer는 종료 시각을 포함한 표본과 left-hold 시간 가중치를 사용한다. commnav는 0–1435분의 5분 간격 288개 표본 비율을 사용한다. 공통 균일 간격에서는 비율이 대응할 수 있지만, 임의 간격·마지막 구간·단절 길이로 확장할 때는 공통 집계 규칙이 필요하다. 항법 계산 불능 표본은 목표 충족률 분모에 남겨야 한다.

기존 `bounded_compute`가 결과 메타데이터에 `geometric_visibility`와 `left_hold_intervals`를 고정 기입하므로 새 COMM/PNT API에 그대로 적용하면 잘못된 설명이 붙을 수 있다. 계산량 제한과 지표별 메타데이터 생성을 분리한다.

근거: `app/core/sampling.py:26`, `app/main.py:78`, `C:/kleo/commnav/dist/engine.js:240`, `C:/kleo/commnav/dist/analysis-worker.js`.

### 4. 통신 모델의 변환

Orbit Designer는 수신 이득과 시스템 잡음온도, 고정 데이터율에서 Eb/N0 마진을 계산한다. commnav는 G/T와 대역폭, 강우손실을 받아 처리량을 계산한다. G/T는 `수신 이득 − 10 log10(잡음온도)`로 대응시킬 수 있지만 데이터율과 대역폭은 같은 설정이 아니다. 기타손실과 강우손실의 중복 적용도 방지해야 한다.

두 결과는 공통 RF 입력에서 계산하는 서로 다른 출력으로 다룬다. commnav의 최선 위성은 항법 탑재 위성의 시간 차감까지 반영하므로, Orbit Designer의 최고 고도각 위성과 항상 같지는 않다. 가시성 판정과 통신 접속 선택 정책도 분리한다.

근거: `app/core/link_budget.py`, `app/core/models.py:28`, `C:/kleo/commnav/dist/engine.js:161`.

### 5. 관측지와 시나리오 저장

기존 `kleo.scenario.v1`에는 COMM/PNT 설정이 없다. 더 중요한 점은 `/api/scenario/validate`가 국가 선택 결과로 `configuration.stations`를 다시 구성한다는 것이다. commnav의 임의 위도·경도를 여기에 넣으면 저장·복원 과정에서 바뀔 수 있다.

새 스키마는 `constellation`, `observers`, `analysis`, `communications`, `navigation`, `display` 등으로 책임을 나누고 관측지 출처를 도시 목록/직접 입력으로 구분한다. 통신·항법 최소 고도각도 각각 보존한다. 기존 JSON과 commnav URL 설정은 변환기로 읽는다. 스키마 버전과 계산 모델 버전을 별도로 기록한다.

근거: `app/main.py:664`, `app/main.py:695`, `examples/scenario_v1_2.json`, `C:/kleo/commnav/dist/app.js:6`.

## 권장 구성

```text
통합 시나리오 · 관측지 · 시간 상태
          │
          ├─ 궤도 상태 공급기: Walker / Multi-shell / TLE / 이상화 항법군
          │       ├─ 가시성 · 히트맵 · 경로
          │       ├─ 통신 링크 · 처리량 · 도플러
          │       └─ 항법 공분산 · HRMS · PDOP
          │
          └─ Cesium 지도 · 하늘보기 · 기간 분석 · 후보 비교 · 내보내기
```

초기에는 commnav의 `prepareGeometry`와 `evaluateSnapshot` 경계가 재사용 지점이다. 공통 전파 상태를 관측지별 기하 데이터로 변환하면, 궤도 생성과 COMM/PNT 평가를 분리할 수 있다. GNSS·지역항법군과 위성별 탑재 속성도 함께 공급해야 한다.

commnav의 JavaScript 평가기·Worker를 유지하고, 공통 Walker/J2 전파와 Orbit Designer의 가시성·기간 집계를 브라우저로 이식한다. Worker 내부에서 궤도 상태를 재사용하고 화면에는 선택 시각의 상태와 집계 결과를 전달한다. 다지역·다후보 분석은 청크 계산, 취소, 작업량 제한을 적용하고 브라우저에서 검증한다. 기존 Python 코드는 개발 과정의 수치 검증 기준으로 활용한다.

기본 배포물은 정적 웹 앱으로 구성한다. 웹 배포에서는 사용자 Python 설치 없이 사용하고, 오프라인 설치는 자산 캐시와 업데이트 정책을 갖춘 PWA 또는 로컬 정적 파일 제공 방식으로 보완한다. 현재 commnav에 설치형 PWA 기능이 구현된 것은 아니다. FastAPI는 기본 실행의 필수 구성에서 제외하며, 대규모 분석의 별도 실행 수단은 실제 수요와 측정 이후 판단한다.

## 단계별 진행안과 완료 기준

1. **공통 설정과 결과 의미 정리:** 새 스키마·기존 설정 변환기·위성 ID·단위를 정의한다. 최초 국가 선택은 한국만 사용하되 불러온 선택을 보존한다. 직접 입력 관측지, 항법 설정, 표시 설정이 왕복 저장 후 유지되고 기존 지표가 모두 표시되어야 한다.
2. **공통 Walker/J2 엔진:** 브라우저 전파기에 Walker F와 J2 RAAN 보정, 일관된 ECEF 위치·속도를 구현한다. J2 끔 조건의 기존 일치성을 보존하고, J2 켬 조건에서는 Python 위치 및 위치의 수치 미분으로 속도·거리 변화율을 검증한다.
3. **통합 화면과 공통 궤도 상태:** Cesium에 통신 접속/항법 사용 위성을 표시하고 하늘보기와 분석 탭을 추가한다. 지도·지표·CSV가 동일한 시나리오와 시간을 사용하고 변경 전 결과를 혼동하지 않아야 한다.
4. **기간·후보 비교 통합:** 궤도 후보별 가시성, 처리량, HRMS, 동시 목표 충족률을 같은 기간으로 평가한다. 현재 시각 민감도와 기간 통계를 구분하고, 비용 자료가 없으면 위성 수를 비용 자체로 표현하지 않는다.
5. **확장 모드 검증:** Multi-shell·TLE 및 여러 관측지의 COMM/PNT 분석을 순차 지원한다. 속도·좌표계·탑재 위성 선택, 계산량·취소·과거 결과 무효화를 검증한다. TLE/SGP4에 Walker용 J2 보정을 추가 중복 적용하지 않는다.

첫 구현 범위는 1–2단계가 적절하다. 이후 단일 화면과 확장 모드로 진행하면 모델 차이와 UI 개편 문제를 각각 검증할 수 있다. 공수는 목표 지원 모드와 정적 독립판 유지 여부에 따라 달라지므로 이번 코드 검토만으로 확정하지 않았다.

## 실행한 검증과 한계

- commnav: `node --test --test-reporter=dot tests/engine.test.js tests/app.test.js tests/worker.test.js` 통과, 총 36개 테스트.
- Orbit Designer: 궤도·기하·링크·샘플링 회귀·API 관련 5개 테스트 파일, 31개 통과. 기존 TestClient 의존성 관련 deprecation warning 1건.
- 위의 두 실제 전파 모듈 간 6개 위성군·5개 시각 교차 비교 수행.
- 최초 검토에서 전체 Orbit Designer 회귀 테스트, 브라우저 시각 QA, 통합 성능·배포 검증은 수행하지 않았다. 후속 검토에서는 초기 국가 선택만 한국으로 변경했다. 단독 실행 측정 범위는 별도 검토 문서에 명시했다.

현재 기능 중 이상화 GNSS·지역항법군, 단순 RF 모델, 표본 기반 지표의 한계는 통합만으로 해소되지 않는다. 통합판도 우선 설계 비교 도구로 정의하는 것이 현재 코드 범위와 일치한다.
